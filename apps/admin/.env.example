NEXT_PUBLIC_API_URL=http://localhost:3000
# Set this to ADMIN, MANAGER, FINANCE, or BUSINESS when each backend is deployed separately.
# Local multi-port startup maps roles by port automatically.
NEXT_PUBLIC_BACKEND_ROLE=
